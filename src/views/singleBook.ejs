<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %>: <%= item.title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="no-scroll">
  <%- include('partials/header') %>
  <div class="container">

    
    <div class="book-detail">
      <% if (item) { %>
        <div class="book-info"> 
          <h1><%= item.title %></h1>
          <p><strong>Author:</strong> <%= item.author %></p>
          <p><strong>Year:</strong> <%= item.year || 'No year' %></p>
          <p><strong>ISBN:</strong> <%= item.isbn || 'No available' %></p>
          <p><strong>Category:</strong> <%= item.category_name || 'No category' %></p>
          <p><strong>Description:</strong> <%= item.description || 'No description' %></p>
        </div>
        


        <div class="actions">
          <a href="/update/<%= item.id %>" class="btn btn-warning">Edit</a>
          <form action="/delete/<%= item.id %>" method="POST" style="display: inline;">
            <button type="submit" onclick="return confirm('¿Delete this book?')">
              Delete
            </button>
          </form>




        </div>
      <% } else { %>
        <p>Book not found</p>
      <% } %>
    </div>
  </div>
</body>
</html>